# sherrydmn

simple daemon that sets the CPU governor to performance when Proton/Wine
processes are running. inspired by [falcond](https://github.com/pikaos-linux/falcond).

depends on power-profiles-daemon.

```
./setup.sh install
./setup.sh uninstall
```
